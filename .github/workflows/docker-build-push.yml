name: Build & Push Docker Image

on:
  push:
    branches: [ "main", "dev" ]

# Tell GitHub Actions to use the newer Node 24 to get rid of the deprecation warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Convert owner to lowercase
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          # Point the context to your app folder where the Dockerfile lives
          context: ./shanghai-15min-app
          push: true
          # Tag the image with your repository name and 'latest'
          tags: ghcr.io/${{ env.OWNER_LC }}/shanghai-15min-app:latest